# Core Application
streamlit==1.45.1

# RAG Pipeline
langchain==0.3.25
chromadb==1.0.12
langchain-community==0.3.25
langchain-core==0.3.65
langchain-text-splitters==0.3.8
sentence-transformers==4.1.0

# Document Processing
pypdf==3.0.1
pdf2image==1.17.0
pytesseract==0.3.13  # Only if using OCR

# Utilities
python-dotenv==1.1.0
numpy==2.2.4
pydantic==2.11.7
tqdm==4.67.1
requests==2.32.3

# Optional (only include if actually used)
transformers==4.52.4  # If using HuggingFace models
huggingface-hub==0.33.0
fastapi==0.115.9  # Only if API endpoints are needed
uvicorn==0.34.3  # Only if serving API
